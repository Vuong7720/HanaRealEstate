@model IEnumerable<Hana.Models.DataModels.Level>


<div class="wrapper wrapper-content animated fadeInRight">
    <div class="row">
        <div class="col-lg-12">
            <div class="ibox ">
                <div class="ibox-title">
                    <h5>Chức vụ trong hệ thống</h5>
                </div>
                <div class="ibox-content">
                    <div class="row mb-2 ml-0">
                        <a onclick="showInPopup('@Url.Action("CreateOrEdit","Level",new { id = 0 },Context.Request.Scheme)','New Level Permission')" class="btn btn-yellow col-lg-1 mr-2 text-white">Thêm mới</a>
                        @*<input type="search" class="form-control col-lg-4" placeholder="search" />*@
                    </div>

                    <table class="table table-hover table-bordered">
                        <thead>
                            <tr>
                                <th class="text-center">
                                    Id
                                </th>
                                <th class="text-center">
                                    Tên chức vụ
                                </th>
                                <th class="text-center">Thao tác</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td class="text-center">
                                        @item.Id
                                    </td>
                                    <td class="text-center">@item.LevelName</td>
                                    <td class="text-center">
                                        <a onclick="showInPopup('@Url.Action("CreateOrEdit","Level",new {id=item.Id},Context.Request.Scheme)','Update level name')" class="btn btn-blue btn-circle text-white"><i class="fas fa-pen"></i></a>

                                        <form asp-area="AdminArea" asp-action="Delete" asp-route-id="@item.Id" onsubmit="return jQueryAjaxDelete(this);" class="d-inline">
                                            <button class="btn btn-danger btn-circle" type="submit">
                                                <i class="fas fa-times"></i>
                                            </button>
                                        </form>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>

                </div>
            </div>
        </div>
    </div>

</div>